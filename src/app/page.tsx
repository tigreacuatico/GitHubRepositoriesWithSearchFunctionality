import SearchBar from "@/components/SearchBar";

export default function Home() {
  return (
    <div>
      <SearchBar />
    </div>
  );
}

// quan clica enter, cal que el porti al perfil del username que ha typed si existeix
